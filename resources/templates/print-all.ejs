<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <title>Печать — Задание <%= taskName %></title>
  <style>
    @page { size:A4; margin:10mm 10mm; }
    body { background:#fff; font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Arial,sans-serif; font-size:13px; color:#111; }
    .doc { page-break-after: always; }
    .doc:last-child { page-break-after: auto; }
  </style>
  <script>
    window.addEventListener('load', () => setTimeout(() => window.print(), 300));
  </script>
</head>
<body>
  <% if (!sections.length) { %>
    <p style="font-size:14px;color:#555;">Нет документов для печати (склад и продавец исключены).</p>
  <% } %>
  <% sections.forEach(function(html){ %>
    <div class="doc"><%- html %></div>
  <% }) %>
</body>
</html>